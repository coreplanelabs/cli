import { telemetryStatusCommand } from './status';
import { telemetryEnableCommand } from './enable';
import { telemetryDisableCommand } from './disable';

export const telemetryCommands = [telemetryStatusCommand, telemetryEnableCommand, telemetryDisableCommand];
